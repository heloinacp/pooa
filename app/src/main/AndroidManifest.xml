<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.iff.pooa2016.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Cadastrarlanche" />
        <activity android:name=".ListarLanches" />
        <activity android:name=".LancheView" />
        <activity android:name=".MainAdminActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CadastroUsuario"></activity>
    </application>

</manifest>